# Infrastructure Notes

This folder is reserved for local environment setup, deployment support files, and operational helpers that are shared across the project.

## Intended Subfolders

- `docker`: local containers and service dependencies
- `env`: documented environment templates and setup notes
- `scripts`: repeatable helper scripts for local setup or CI

The project is not using deployment code here yet, but this structure gives those assets a clear home once they are needed.
