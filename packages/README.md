# Shared Packages

This folder holds project assets that are meant to be reused by multiple apps.

Current planned package areas:

- `contracts`: shared API and event definitions
- `prompts`: structured prompt assets and evaluation rubrics
