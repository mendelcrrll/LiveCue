# Contracts Package

This package is reserved for shared API and event definitions used by both the frontend and backend.

Planned contents:

- request and response schema references
- live event names
- payload shape documentation
- generated or hand-maintained shared types if needed later

The goal is to keep the frontend and backend aligned without coupling either side to provider-specific payloads.
